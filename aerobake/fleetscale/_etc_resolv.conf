# Managed statically — do not edit by hand and do not let dhcpcd overwrite.
# dhcpcd is configured with 'nohook resolv.conf'; the upstream VPC resolver
# (169.254.169.253) is set in /etc/dnsmasq.conf.
nameserver 127.0.0.1
