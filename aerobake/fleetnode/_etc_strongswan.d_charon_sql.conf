sql {
	load = no

	# The StrongSwan SQL plugin is not used on fleetnode (Debian 12 Bookworm).
	# Connection configs and PSK credentials are managed by ipsecnode
	# via the VICI interface directly (load-shared / unload-shared commands).
	# This entry exists only to suppress the startup warning.
	loglevel = 0
}
